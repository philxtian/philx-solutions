{
  "name": "philx-solutions",
  "private": true,
  "version": "1.0.0",
  "description": "PHILX Solutions marketing site — static HTML/htmx frontend with a Tailwind CSS build step.",
  "scripts": {
    "build": "tailwindcss -i ./src/tailwind-input.css -o ./assets/tailwind.css --minify",
    "watch": "tailwindcss -i ./src/tailwind-input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
